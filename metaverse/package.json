{
  "name": "ecci-metaverse",
  "version": "1.0.0",
  "description": "ECCI BigData Metaverse — Fase 1: Ciudad 3D",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "bun server/index.js",
    "test": "bun test",
    "kafka:up": "docker compose up -d",
    "kafka:down": "docker compose down",
    "kafka:tail": "docker compose exec kafka /opt/kafka/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic route.decision --from-beginning"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "three": "^0.165.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}
